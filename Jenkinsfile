This defines a jenkins pipeline for building and deploying project.
the pipeline uses configuration from a remote YAML file and enables CI/CD.
This will contain all the build,scan and deployment process for your application
